import { TodoFilterPipePipe } from './todo-filter-pipe.pipe';

describe('TodoFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TodoFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
